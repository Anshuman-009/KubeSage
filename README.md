# POC-RTML-AGENT
Understand how a trained ML model becomes useful inside a live system, how RAG injects the right evidence at the right time, how WebSocket alerts behave during a telemetry spike, and how different LLMs perform under the same context.
